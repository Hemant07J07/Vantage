fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.9.2
ddgs==9.0.0
ollama==0.6.0
python-dotenv==1.0.1
# HTML -> text for the pages behind search results. Rust-backed; at 20 pages
# per research step inside a 150s budget, BeautifulSoup's parse time is the
# difference between fitting and not.
selectolax==0.3.21
